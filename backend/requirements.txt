# backend/requirements.txt

# Core Framework - Updated versions compatible with Python 3.13
fastapi==0.115.0
uvicorn[standard]==0.30.0

# Database
sqlalchemy==2.0.35

# Database migrations
alembic==1.13.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12

# Environment Variables
python-dotenv==1.0.1

# Validation - Updated to Python 3.13 compatible version
pydantic==2.9.0
pydantic[email]==2.9.0
email-validator==2.2.0

# PDF invoices (pure Python — no system libraries, so it builds on Render)
reportlab==5.0.1

# Password hashing for the billing portal.
# NOTE: passlib 1.7.4 breaks with bcrypt 5.x (it reads bcrypt.__about__), so
# app/security.py uses the bcrypt library directly.
bcrypt==5.0.0
